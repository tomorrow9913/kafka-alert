<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; border: 1px solid #ddd; border-radius: 5px; }
        .header { background-color: #f8d7da; color: #721c24; padding: 10px; border-radius: 5px 5px 0 0; }
        .content { padding: 20px 0; }
        .footer { font-size: 0.8em; color: #777; text-align: center; margin-top: 20px; }
        .meta { background-color: #f9f9f9; padding: 10px; border-radius: 5px; margin-top: 10px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>🚨 Alert Notification</h2>
        </div>
        <div class="content">
            <p><strong>Service:</strong> {{ service }}</p>
            <p><strong>Message:</strong> {{ message }}</p>
            
            {% if error_code %}
            <div class="meta">
                <p><strong>Error Code:</strong> {{ error_code }}</p>
            </div>
            {% endif %}
            
            {% if details %}
            <h3>Details:</h3>
            <pre>{{ details }}</pre>
            {% endif %}
        </div>
        <div class="footer">
            <p>Generated by Kafka Alert System</p>
            <p>{{ timestamp }}</p>
        </div>
    </div>
</body>
</html>
